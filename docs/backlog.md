# norma backlog

**这份文件是「要做什么」，不是「怎么工作」。** 怎么工作在 `docs/HANDBOOK.md`。

分三类写，因为它们的处理方式完全不同：

- **一、使用摩擦** —— 真实使用中撞到的，带现场。→ 决定 v2 该做什么
- **二、待清** —— 已知要做的小项，不紧急。→ 顺手做
- **三、想法** —— 还没验证的。→ **先别做**

> **已知天花板**（那些是*刻意*的取舍，不是待办）在
> `docs/superpowers/specs/2026-09-15-norma-agent-kernel-design.md` 的 §14，这里不重复。

---

## 一、使用摩擦（用 norma 干真活时撞到的）

**怎么记**：一行就够——你在干什么、它做了什么、哪里别扭。**不要写分析**，分析留到设计 v2 时做。

| 日期 | 场景 | 撞到什么 | 影响 |
|---|---|---|---|
| 2026-09-16 | 冒烟：列出下载文件夹 | 先用 `C:\Users\%USERNAME%\Downloads` 当路径（`%VAR%` 只在 cmd.exe 里展开），自己改道成功 | 无——自愈了。但路径类提示可以更好 |
| 2026-09-16 | 冒烟：问答语种 | 中文提问，模型第一句用英文回答再转中文 | 轻度别扭。**没有 system prompt**（见「想法」） |
| 2026-09-19 | 读小说（6 次，3 档规模） | **每次开场先弹一记权限询问**——它用 `run_powershell` 去"找文件"，而 `list_dir` 明明已经列出来了 | 体验很差：让它读个文件，第一眼看到的是吓人的确认框 |
| 2026-09-19 | 同上 | 被拒后它理解成"**PowerShell 这台机器上不可用**"，连着试了 4 种命令（`Substring`/`Select-String`/`Get-Content`/最后拿 `echo test` 试探环境） | 4 步烧在"确认环境坏没坏"上。回填文案「用户拒绝了这次操作」没告诉它**换个工具** |
| 2026-09-19 | 同上 | `norma.log` 写在 CWD，出现在 `list_dir` 结果里，**模型去读它当内容** | 白烧一步。它自己纠正了，但不该发生 |
| 2026-09-19 | 同上 | 它试了 `max_bytes` = 6万 / 20万 / 40万，**返回都一样**（开头约 7000 字） | 参数在骗人，诱导它浪费步数（见「待清」头条） |
| 2026-09-19 | 同上 | ✅ 好的一面：**截断标记起作用了**，它看见 `…[已截断，原文 N 字符]` 并正确诊断 | 说明 `truncate()` 那层是好的 |
| 2026-09-19 | 同上 | ✅ 好的一面：它**明确拒绝编造**——"必须先说明一个硬性限制，避免我编造后面的内容" | 抗幻觉行为正确。对小说改编游戏极重要 |
|  |  |  |  |

> 完整分析见 `E:\learning\ai\whispering-verse-game\_norma-experiment\FINDINGS.md`（含原始记录）。

---

## 二、待清（已知要做，排在后面）

这些都是 v1 的审查里记下但**按流程刻意没修**的项。ledger 已随 SDD 工作区删除，
这份清单是它们的落点——**别让它们再丢一次**。

### 来自真实使用（有实测数据，优先做）

**（空了）** —— 读小说实验（2026-09-19）记下的摩擦，到 v1.1.0 为止全部做完：
`max_bytes` / `norma.log` 位置 / 拒绝文案（v1.0.1）、system prompt（v1.0.1）、
区间读取（v1.1.0）。**下一步的方向在 spec §17（v2 传输层）**，
以及下面「来自 v1 审查」那批没有实测数据的顺手项。

### 来自 v1 审查（无实测数据，顺手做）

- [ ] **`_list_dir` 的 `iterdir()`/`stat()` 异常未加保护** —— 一个权限不足的子项会毁掉整个列表。
      逐项 try，或至少返回「部分列出」。
- [ ] **审计日志的 `args` 未截断** —— `write_file` 的 content 会让日志涨到 MB 级。加个上限。
- [ ] **`POLICY[tool.risk]` 是裸下标** —— 未来新增 `Risk` 成员会在该工具被用到时抛 `KeyError`。
      加一条不变量测试 `assert set(POLICY) == set(Risk)`（见 spec §14）。
- [ ] **`permission.py` docstring 措辞** —— 把「手机远程确认」列为未来实现，可能被读成
      「裁决可以放到远端」。补一句「远程只传答案，判断仍在本地」。
- [ ] **终结事件断言只看 `events[-1]`** —— 一个发出 N 个终结事件的变异能存活。
      加 `assert len(events) == 1` 或计数 `Failed`。
- [ ] **`reply is None` 分支无测试** —— `FakeLLM` 永远产出 `Reply`，加一个空轮次的剧本。
- [ ] **审计断言只数条数、不辨身份** —— 三条不同结局刚好凑够 3。加身份断言更稳。
- [ ] **`test_execute_uses_param_defaults` 单独看是弱测试** —— `_echo` 本就有默认值，
      不传参也通过；与 `test_execute_success` 成对才有效。加注释说明。
- [ ] **`tests/test_llm.py` 的 `_llm_capturing` 不 close 客户端** —— 测试层资源小瑕疵。

---

## 三、想法（还没验证，**先别做**）

- [ ] **会话持久化** —— 每个会话从零开始。但「每个任务：读几个文件 → 产出一个文件」
      可能已经够用。**等真实使用数据**。
- [ ] **上下文压缩** —— 同上，先看文件系统方案够不够。
- [ ] **`run_shell` 按平台选 shell** —— Windows → PowerShell，POSIX → `sh`。
      macOS 支持的正确形态（见 spec §17）。等有 Mac 验证条件时再做。
- [ ] **打包（PyInstaller 冻结）** —— 让分享变成「下载一个安装包」。与客户端绑定，排在 v2 后。
