# norma backlog

**这份文件是跨会话的记忆。** 任何人（你、我、将来的 norma）在使用或开发中撞到东西，
就往这里加一行；**下一次会话开工前先读它**。

分三类写，因为它们的处理方式完全不同：

- **一、使用摩擦** —— 真实使用中撞到的，带现场。→ 决定 v2 该做什么
- **二、待清** —— 已知要做的小项，不紧急。→ 顺手做
- **三、想法** —— 还没验证的。→ **先别做**

> **已知天花板**（那些是*刻意*的取舍，不是待办）在
> `docs/superpowers/specs/2026-09-15-norma-agent-kernel-design.md` 的 §14，这里不重复。

---

## 一、使用摩擦（用 norma 干真活时撞到的）

**怎么记**：一行就够——你在干什么、它做了什么、哪里别扭。**不要写分析**，分析留到设计 v2 时做。

| 日期 | 场景 | 撞到什么 | 影响 |
|---|---|---|---|
| 2026-09-16 | 冒烟：列出下载文件夹 | 先用 `C:\Users\%USERNAME%\Downloads` 当路径（`%VAR%` 只在 cmd.exe 里展开），自己改道成功 | 无——自愈了。但路径类提示可以更好 |
| 2026-09-16 | 冒烟：问答语种 | 中文提问，模型第一句用英文回答再转中文 | 轻度别扭。**没有 system prompt**（见「想法」） |
| 2026-09-19 | 读小说（6 次，3 档规模） | **每次开场先弹一记权限询问**——它用 `run_powershell` 去"找文件"，而 `list_dir` 明明已经列出来了 | 体验很差：让它读个文件，第一眼看到的是吓人的确认框 |
| 2026-09-19 | 同上 | 被拒后它理解成"**PowerShell 这台机器上不可用**"，连着试了 4 种命令（`Substring`/`Select-String`/`Get-Content`/最后拿 `echo test` 试探环境） | 4 步烧在"确认环境坏没坏"上。回填文案「用户拒绝了这次操作」没告诉它**换个工具** |
| 2026-09-19 | 同上 | `norma.log` 写在 CWD，出现在 `list_dir` 结果里，**模型去读它当内容** | 白烧一步。它自己纠正了，但不该发生 |
| 2026-09-19 | 同上 | 它试了 `max_bytes` = 6万 / 20万 / 40万，**返回都一样**（开头约 7000 字） | 参数在骗人，诱导它浪费步数（见「待清」头条） |
| 2026-09-19 | 同上 | ✅ 好的一面：**截断标记起作用了**，它看见 `…[已截断，原文 N 字符]` 并正确诊断 | 说明 `truncate()` 那层是好的 |
| 2026-09-19 | 同上 | ✅ 好的一面：它**明确拒绝编造**——"必须先说明一个硬性限制，避免我编造后面的内容" | 抗幻觉行为正确。对小说改编游戏极重要 |
|  |  |  |  |

> 完整分析见 `E:\learning\ai\whispering-verse-game\_norma-experiment\FINDINGS.md`（含原始记录）。

---

## 二、待清（已知要做，排在后面）

这些都是 v1 的审查里记下但**按流程刻意没修**的项。ledger 已随 SDD 工作区删除，
这份清单是它们的落点——**别让它们再丢一次**。

### 来自真实使用（有实测数据，优先做）

- [ ] **`read_file` 的 `max_bytes` 是假参数** —— 真正的闸门是 `truncate()` 的
      `MAX_RESULT_CHARS = 8000` 字符，而它在 `max_bytes` **之后**才生效。
      于是对中文（3 字节/字），`max_bytes` 超过约 24000 就完全无效。
      实测：模型试了 6万/20万/40万，返回都是开头约 7000 字，**它因此浪费了数步**。
      两个方向选一个：**要么让 `max_bytes` 真的管用**（结果上限跟着它走），
      **要么删掉这个参数**（承认"每次最多 8000 字符"是硬事实）。
      现状最糟——它既不是真的，又会被模型当成真的。
- [ ] **`read_file` 加 `offset`/`limit`（区间读取）** —— 现在**只能从文件开头读**，
      读不到任何非开头部分。模型自己诊断出了这一点（原话：
      "`read_file` 只能从文件开头返回内容，没有 offset 参数"）。
      **这是小说场景的硬阻塞**，也是从「想法」升级上来的——因为现在有数据了。
- [ ] **`norma.log` 不该写在 CWD** —— 它会出现在 `list_dir` 结果里、被模型当内容读、
      也会污染用户的工作目录。挪到固定位置（`~/.norma/audit.log` 之类）。
- [ ] **权限被拒的文案应该更可操作** —— 现在只说「用户拒绝了这次操作」，模型会理解成
      "这个工具坏了"，然后花好几步确认环境。改成能引导它换工具的措辞
      （比如「用户拒绝了这次 **run_powershell** 调用，请改用其他工具」）。
      **v2 设计权限往返时要吃这一条。**

### 来自 v1 审查（无实测数据，顺手做）

- [ ] **`_list_dir` 的 `iterdir()`/`stat()` 异常未加保护** —— 一个权限不足的子项会毁掉整个列表。
      逐项 try，或至少返回「部分列出」。
- [ ] **审计日志的 `args` 未截断** —— `write_file` 的 content 会让日志涨到 MB 级。加个上限。
- [ ] **`POLICY[tool.risk]` 是裸下标** —— 未来新增 `Risk` 成员会在该工具被用到时抛 `KeyError`。
      加一条不变量测试 `assert set(POLICY) == set(Risk)`（见 spec §14）。
- [ ] **`permission.py` docstring 措辞** —— 把「手机远程确认」列为未来实现，可能被读成
      「裁决可以放到远端」。补一句「远程只传答案，判断仍在本地」。
- [ ] **终结事件断言只看 `events[-1]`** —— 一个发出 N 个终结事件的变异能存活。
      加 `assert len(events) == 1` 或计数 `Failed`。
- [ ] **「`max_steps` 计模型回合而非工具调用」未被测试钉住** —— 补一条「一轮多个工具调用 +
      小 `max_steps`」的用例；否则按工具调用计数的实现也能全绿。
- [ ] **`reply is None` 分支无测试** —— `FakeLLM` 永远产出 `Reply`，加一个空轮次的剧本。
- [ ] **审计断言只数条数、不辨身份** —— 三条不同结局刚好凑够 3。加身份断言更稳。
- [ ] **`test_execute_uses_param_defaults` 单独看是弱测试** —— `_echo` 本就有默认值，
      不传参也通过；与 `test_execute_success` 成对才有效。加注释说明。
- [ ] **`tests/test_llm.py` 的 `_llm_capturing` 不 close 客户端** —— 测试层资源小瑕疵。
- [ ] **`README.md` 的「加一个工具」说「加一行」** —— 实际需要
      `name`/`description`/`params`/`risk`/`fn` 五项，措辞改准。
- [ ] **`_read_file` 截断时没有 `[已截断]` 标记** —— 模型不知道看到的是残片。
- [ ] **`_read_file` 的 `max_bytes ∈ {1,2}` 时中文仍报二进制** —— 切片不足一个字符，
      唯一替代答案是返回 `""`（另一种错）。当前可接受，记录备查。

---

## 三、想法（还没验证，**先别做**）

- [ ] **加 system prompt** —— 目前完全没有。冒烟时模型用英文回答中文提问，一句
      「始终用中文」可能就够。成本极低，可以先做。
- [ ] ~~`read_file` 支持区间读取~~ —— **已升级到「待清」**（2026-09-19 实验给了数据支撑）。
- [ ] **会话持久化** —— 每个会话从零开始。但「每个任务：读几个文件 → 产出一个文件」
      可能已经够用。**等真实使用数据**。
- [ ] **上下文压缩** —— 同上，先看文件系统方案够不够。
- [ ] **`run_shell` 按平台选 shell** —— Windows → PowerShell，POSIX → `sh`。
      macOS 支持的正确形态（见 spec §17）。等有 Mac 验证条件时再做。
- [ ] **打包（PyInstaller 冻结）** —— 让分享变成「下载一个安装包」。与客户端绑定，排在 v2 后。

---

## 四、本机环境（换会话/换机器时先看这一段）

这台机器有几个坑，踩过一遍，别再踩：

| 坑 | 对策 |
|---|---|
| `python` 是 **0 字节的 Microsoft Store 占位程序**，运行返回 9009 | 一律用 **`E:\learning\ai\norma\.venv\Scripts\python.exe`**；`norma` 命令用 `.venv\Scripts\norma.exe` |
| 真实 Python 在 `E:\tools\python\`，**不在 PATH** | 同上。建 venv 用 `E:\tools\python\python.exe -m venv .venv` |
| `origin` 指向 `githubproxy.cc`，**它只代理拉取，不支持 push** | 推送走本机 Clash：<br>`git -c http.proxy=http://127.0.0.1:7890 push https://github.com/DestinyMiner/norma.git main` |
| **Clash 占着系统代理（127.0.0.1:7890）**，而 `httpx2` 认 `ProxyServer` 却不认 `ProxyOverride` 的 `127.*` | 发往 localhost 的请求要显式 `NO_PROXY=127.0.0.1,localhost`。测试已自带（`tests/test_cli.py`）；**将来把 `NORMA_BASE_URL` 指向本地模型时会踩到**（见 spec §14） |
| 用 PowerShell 的 `Get-Content`/`Set-Content` 读写源码会**按 GBK 往返破坏 UTF-8** | 改文件一律用编辑工具，不要用 PowerShell 文本 cmdlet |
| PowerShell 的 `>` 会把原生命令输出**转码成 UTF-16LE** | 要检查真实字节就用 Python 子进程的裸句柄 |

**另外**：这个会话里曾经把小说按章切好放在
`E:\learning\ai\whispering-verse-game\game-script\chapters\`（00-设定 / 01-序章 / 02-第一卷 / 03-第二卷），
每章都在诺玛 8000 字符的读取上限内。

---

## 五、会话怎么切

**按任务切，不按角色切。** 会话的单位是「**一个能关闭的目标**」，不是「一个职位」。

| 会话类型 | 什么时候开 | 开场白 | 什么时候算完 |
|---|---|---|---|
| **里程碑** | 要做新东西（如传输层） | 「读这份文件和 spec §17，我们来做 X」 | 走完设计→计划→实现 → 打 tag → 更新这份文件 |
| **清扫** | 「待清」攒了几条 | 「读这份文件，清掉这几条」 | 那几行删掉 |
| **方向** | 每个里程碑**开始前** | 「读 spec + 这份文件 + `git log`，问：**我们还在做对的事吗？**」 | 结论写进 spec |

**「方向」这一类是唯一容易被跳过的，但最值钱**：里程碑会话审的是「你有没有按计划做」，
**没有人在审「计划本身对不对」**。一次方向会话 = 一场短对话，换来的是避免做错整个里程碑。

**不要为「监督」建常驻 agent。** 监督已经在流程里了（每个任务都有独立审查者）。
常驻角色会变成你要维护的另一件事。

## 六、分工（人和 AI）

| | 谁做 | 产出 |
|---|---|---|
| **实验** | AI | 可复现的机制记录：跑几次、几步、第几步崩、什么时候开始重复劳动 |
| **使用** | 人 | **三行现场**：我想干什么 / 它干了什么 / 别扭在哪 |

**「别扭」是人独有的数据**——AI 采集不到，因为它没有体感。反过来，「跑 5 次几次成功」是 AI 独有的——人没必要手搓。

**人不需要写 bug 报告。** 诺玛已经把每次工具调用写进 `norma.log` 了；
人只要产出现场，**分析是下一阶段的活**。

## 七、版本与提交约定

> **这是个人约定，跨仓库通用**，不只在 norma 里。新建仓库时把这节抄过去。

版本号三位 `X.Y.Z`：

| 位 | 含义 | 例 |
|---|---|---|
| **X** 第一位 | **底层框架改变** | 加传输层（内核多了第二种运行形态 + 一套对外协议）→ `2.0.0` |
| **Y** 第二位 | **功能新增或重构** | `read_file` 加区间读取、加 system prompt → `1.1.0` |
| **Z** 第三位 | **bug 修复和优化** | 日志位置、拒绝文案、`max_bytes` 说清楚 → `1.0.1` |

**判断规矩**——拿到一个改动，先问一句：

```
「这是在加能力，还是把已有的修对？」
    加能力            → 动第二位
    修对              → 动第三位
    改变底层怎么跑    → 动第一位
```

**和标准 semver 的差别**（容易搞混，记一下）：标准 semver 的第一位是「**破坏性**变更」，
这里的第一位是「**底层框架**改变」。所以「重构但接口没破」在这里动**第二位**。

**发布动作**（norma 的做法，别的仓参照）：

1. 同时升 `pyproject.toml` 的 `version` 和 `src/norma/__init__.py` 的 `__version__`——两处必须一致，否则 `pip show` 和 tag 会打架
2. 用**附注 tag**（`git tag -a vX.Y.Z`），把「这个版本承诺什么、不承诺什么」写进 tag 说明——将来 `git tag -n99 -l` 就能看回来
3. 推送按「四、本机环境」里的 Clash 命令走

## 八、会话开工前读什么

1. **这份文件**（3 分钟）
2. `docs/superpowers/specs/2026-09-15-norma-agent-kernel-design.md` 的 **§17**
   （v2 做什么、不做什么、三个待答的设计问题）
3. `git log --oneline -20`（最近发生了什么）

**这三份读完就能冷启动，跟你隔了 1 天还是 2 周无关。** 所以：**长会话可以放心关掉**——
决策该由文件驱动，不该由"我还记得聊过什么"驱动。

## 九、会话收尾时做什么

**把状态写进文件，不要留给下一次对话去记。** 具体地：

- 撞到的摩擦 → 加进上面第一张表
- 决定要做的 → 进「待清」，或直接进 spec
- 决定不做的 → 进 spec §14 的已知取舍表（**带上升级路径**）
- 做完的 → 删掉对应行
- **只在对话里说过的策略/约定 → 补进这份文件**（这一节就是这么来的）

> 一份没人读的 backlog 等于没有。所以这里只放**真会做**的事——
> 宁可短，宁可空，也不要攒成一份没人看的清单。
